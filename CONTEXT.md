# Comet 工作流语言

Comet 复用宿主 Agent 的推理与工具执行循环，提供可持久化的 Skill 编排。Native、Classic 和自定义工作流拥有各自的业务规则。

## Language

**Run**：一份工作流定义的一次执行实例，可以跨越多个宿主会话。
_Avoid_: 用宿主 Session 代指工作流实例。

**Action**：工作流允许执行的一项工作，具有独立身份、输入绑定和执行尝试。
_Avoid_: 再引入一套与 Action 同义的 Task 名称。

**Outcome**：执行者对某个 Action 的一次结果回报；回报成功不等于业务验收通过。
_Avoid_: 将执行回报直接称为验收证明。

**Wait**：流程等待用户决定或外部条件的持久化事实，与对应提案及恢复条件绑定。

**Checkpoint**：从已提交工作流边界继续所需的事实，不等于宿主模型上下文或文件备份。

**Workflow Definition**：工作流允许的步骤、转换、输入输出与业务规则的版本化定义。

**Host**：提供 Agent 推理、工具执行和会话能力的平台，不是工作流状态的另一份权威存储。
