# 目标

在 rc6 修复 Native 子任务验收保障、跨工作区归档状态发现和规格修订恢复问题，提交 fix PR 并验证最终提交的线上 CI 全部通过。

# 范围

以 GitHub #389、#390、#391 和当前 rc.5 源码、真实 Runtime 复现为依据。389 的本地设计 `docs/architecture/NATIVE-SUPERVISOR-SUBTASK-ACCEPTANCE-UNIFICATION.md` 完整读取，作为实现参考；报告中的假设和建议通过当前实现核实后采用。

# 非目标

不修改 website，不合并 PR，不重造已有局部验收循环，不放开 Runtime 状态或归档证据写入，不为没有复现的 CLI 与 Runtime 版本组合增加独立服务握手。

# 验收示例

- A1：Supervisor 子任务按已确认范围逐项验收，遗漏、重复、未知 ID、结论矛盾及缺少实际通过检查的 pass 均被拒绝；单会话与多会话使用同一规则。
- A2：子任务正式检查由 Runtime 执行并保存可校验的候选、执行身份和内容引用；外部材料登记后校验哈希与绑定，旧候选或篡改材料不能被消费为有效证据；任务包明确范围和未执行检查的原因。
- A3：主工作区归档后，旧 linked worktree 的活跃副本不能覆盖最终完成状态；只有身份和 Git 历史证明替代关系才自动选归档，无法确认时报告冲突。
- A4：当前 change 通过受控入口同步规格交叉引用，记录原因、操作者、内容变更和影响范围；需求变化仍回 Shape，正式项目规格通过当前 change 和 Archive 更新，Runtime 状态及归档证据保持保护。
- A5：需求修订后 brief/spec 可编辑并重新确认；局部修复保留未受影响结果，受影响项重新验收，归档前全量验收不复用为新结果。
- A6：Verifier 失联在恢复入口转为明确执行错误或中断并提供可执行恢复动作；不兼容 schema 在入口拒绝且不写入当前选择或创建无法完成的 change。
- A7：源码、生成 Runtime、双语操作说明、rc.6 版本和英文 changelog 一致；相关回归、构建、生成检查及最终 PR 线上检查通过。

# 约束与不变量

只修改任务文件；状态通过公开 `comet native` 推进。单元、生成 bundle、临时真实 Runtime 和线上 CI 分别记录。既有保障用回归证明，不将报告建议一律当缺陷。

# 决策

- 用户明确授权“不需要再问我怎么做，自己选择最优的修复策略”，据此自主确定范围、确认并完成流程及 PR。
- 在当前干净 rc6 分支使用单一 Native change，三个问题共享 Native 契约和最终验证，避免拆分后反复修改同一区域。
- 389 复用现有逐项校验、检查执行器和内容寻址存储。390 使用稳定身份与 Git 历史而非版本大小排序。391 优先复用现有修订与恢复入口，补齐可证明的同步缺口。
- master 和上一个发布 tag 均为 0.4.0-rc.5，本次发布条目使用 0.4.0-rc.6。

# 待解决问题

无待定用户决定；实施中的事实由最小复现与测试确认。

# 验证预期

相关 Native、CLI、Hook 与 Skill 契约测试，pnpm build、pnpm lint、pnpm check:generated、受影响文件 Prettier；独立只读复核及 Native Verifier。完整 CI 覆盖的全量测试在线上执行并追踪最终 head，失败必须修复后重新验证。
